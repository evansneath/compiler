//////////////////////////////////////////////////////////////////////////////
// File:   testfile.src
// Author: Evan Sneath
//////////////////////////////////////////////////////////////////////////////

// Test newline, space skipping, and comments
     

// Tests keyword, symbol, identifier, and string
string stringTest := "Hello world";

// Test identifier with underscore and integer
int magic_number := 42;

// Test a complex line of keywords, strings, and identifiers
if (condition = true or another = "some string") then

    // Test alphanumeric identifiers and hanging float handling
    // On the case of a hanging float (x.), a zero value (x.0) is assumed
    float cam3l_Case1 := 1.;

    // Test out some strange usage of the float underscore
    float strangeFloat := 2_____._____2______;

    // Test unknown character handling
    bool foo#bar := true;
end

// Test a compact formed if statement
if(foo=bar)then
    do_something();
end

// Test warning handling of unknown characters in strings
string bad_string := "Too# Many $trange characters here?";

// Test empty string handling
string empty_string := "";

// Test hanging string error handling. This sends an irrecoverable error.
string hanging_string := "This is a string that never ends.

